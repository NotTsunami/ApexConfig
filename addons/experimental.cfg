// These are some experimental commands that may or may not improve FPS
// These settings may or may not increase FPS. I only recommend trying this
// if you want to experiment or the toaster config does still not give you
// enough fps.

demo_autoRecord 0 // Disables demo recording (Defaults to 1, but the most
                  // interesting thing being there is no visible system for
                  // recording and playing back demos, needs further testing
                  // to see if this even does anything)
rui_asyncTracks 1 // Toggles async update of RUI tracks
